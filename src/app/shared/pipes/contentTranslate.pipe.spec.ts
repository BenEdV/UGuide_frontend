import { ContentTranslatePipe } from './contentTranslate.pipe';

describe('ContentTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new ContentTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
